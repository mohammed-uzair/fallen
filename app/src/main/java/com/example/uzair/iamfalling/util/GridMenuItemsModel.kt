package com.example.uzair.iamfalling.util

/**
 * This is the model to store the menu items values
 */
data class GridMenuItemsModel(
    val menuId: Int = 0,
    val imageDrawableResource: Int = 0,
    val menuTextResource: Int = 0
)